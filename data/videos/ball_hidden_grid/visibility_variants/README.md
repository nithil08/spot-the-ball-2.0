# Visibility Variants — ball_hidden_grid

3 clips: 4 seconds of play with ball hidden and a 32×12 alphanumeric grid burned in.

| Clip folder | Variant | Scenario | Description |
|-------------|---------|----------|-------------|
| `08fcceebae/` | **Ball Hidden + Grid** | academy_counterattack_easy seed=23 | Ball shrunk to 5% (imperceptible); grid overlay; ground-truth in `meta.json` |
| `666bb5de48/` | **Ball Hidden + Grid** | (see meta.json) | Ball shrunk to 5%; grid overlay; ground-truth in `meta.json` |
| `9066812d07/` | **Ball Hidden + Grid** | (see meta.json) | Ball shrunk to 5%; grid overlay; ground-truth in `meta.json` |

**Visibility type:** Ball is hidden throughout — no visible-ball variant for this batch.
The grid is a 32-column × 12-row alphanumeric overlay (columns 1–32 left-to-right, rows A–L top-to-bottom).

**Generated by:** `data/code/ball_hidden_grid_code.py`
**Original files:** `experiments/nithil_work/results/ball_hidden_grid/`
