# Visibility Variants — ball_hidden_4s

Early probing experiment: a single still-frame PNG captured at 4 seconds into play with the ball hidden.

| Clip folder | Variant | Description |
|-------------|---------|-------------|
| `631bd870fe/` | **Ball Hidden / Freeze at 4s** | `academy_counterattack_easy` seed=23; ball shrunk to invisible; freeze-frame PNG at t=4s saved as `frame_4s.png`; ground-truth ball position recorded in `meta.json` |
| `e9d90747ad/` | **Ball Hidden / Freeze at 4s** | Same scenario/seed (second run); outputs same as above |

**Visibility type:** Ball is hidden (5% scale) throughout — no visible-ball variant for this batch.
This batch was used for initial VLM probing and is superseded by `ball_hidden_grid/`.

**Generated by:** `data/code/ball_hidden_4s_code.py`
**Original files:** `experiments/nithil_work/results/ball_hidden_4s/`
